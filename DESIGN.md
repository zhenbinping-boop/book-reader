# 设计基线

本文件是 book-reader 所有界面决策的**唯一事实来源**。改 UI 前先读这里；代码与本文件冲突时，以本文件为准，然后改代码。

最后更新：2026-09-22

---

## 1. 定位

**纸感（E-Ink / Paper）× 理性网格（Swiss Modernism 2.0）**

一句话解释：**阅读区像纸，操作层像仪器。**

- 阅读区（正文、PDF 页）追求"这是一张纸"——低亮度、无装饰、不发光、不打扰。
- 操作层（书架、工具栏、目录抽屉）追求"这是一个仪器"——网格对齐、层级清晰、触控目标明确、反馈即时。

### 三条不可协商原则

| # | 原则 | 具体含义 | 反例（禁止） |
|---|---|---|---|
| 1 | **内容优先，chrome 退让** | 不需要时 UI 完全消失（阅读器工具栏已实现），正文区域零装饰 | 常驻的悬浮按钮、阅读时可见的边框/水印 |
| 2 | **纸不发光** | 大面积底色一律用低亮度暖色，禁止纯白铺底（阅读区） | 阅读区 `background: #fff`、纯黑配纯白 |
| 3 | **位移优于淡入** | 翻页、抽屉用位移表达空间关系；淡入只用于内容就绪 | 章节切换做 crossfade、滚动视差 |

---

## 2. 依据与偏离记录

配色与风格来自 `ui-ux-pro-max` 技能的产品库条目 **Book & Reading Tracker**（`products.csv` / `colors.csv`）：

- 主风格：Swiss Modernism 2.0 + Minimalism & Swiss Style
- 次风格：E-Ink / Paper、Soft UI Evolution
- 配色方向原文：`Warm paper white + ink brown + reading progress green + book cover colors`

### 三处有意偏离（都有理由，不是偷懒）

| 技能建议 | 本项目的做法 | 理由 |
|---|---|---|
| `--border-radius: 0px`（E-Ink 与 Swiss 都这么建议） | 阅读区 0px，UI 层 10–14px | 触屏上全直角按钮辨识度与命中率都差。技能的 0px 是针对桌面排版工具链的建议，未考虑手指输入。**阅读区仍守 0px，因为那是"纸"** |
| `Dark Mode: not-recommended`（E-Ink/Paper 条目明确标注） | 保留暗色，但**不采用"纯黑+纯白"** | 阅读时长是刚需，不能用"不支持暗色"回避。做法是把纸感原则**映射**到暗色：暗底用 `#14161A` 而非 `#000`，正文用 `#B9B5AE` 而非 `#fff`——牺牲一点对比度换掉暗底光晕（halation） |
| 推荐字体 `Cormorant Garamond` / `Fira Sans` 等 webfont | **一个 webfont 都不用**，全部系统字体栈 | ① PWA 要离线可用，webfont 是网络请求；② 承诺过"文件不出本机"，加载第三方 CDN 会打脸；③ 中文字体（思源宋体 10MB+）不可能打包。技能给的 `--font-reading: Georgia` 本身就是系统字体，这条反而印证了做法 |

### 一处技能数据的实际缺陷

技能在 Book & Reading Tracker 里给的 Accent 是 `#D97706`（页黄）。实测它在纸底 `#FFFBEB` 上只有 **3.07:1**，**不能用作正文或小字号文字色**。本项目因此把 accent 拆成两个用途：

- **图形用途**（进度条填充、选中指示、图表）→ 用 `#C98A2E`，不受文字对比度约束
- **文字用途**（链接、强调文本）→ 用 `#8A5A2B`（亮底）/ `#C9A37A`（暗底），均实测 ≥ 4.5:1

---

## 3. 色彩系统

### 3.1 为什么分两层

这是本项目色彩架构的核心决策：

```
┌─ UI 层 ────────────────────────┐  书架、工具栏、抽屉、按钮
│  跟随系统亮/暗，两套           │  accent = 墨棕（Book brown）
└────────────────────────────────┘
┌─ 阅读区层 ─────────────────────┐  正文底、正文色、页码
│  用户显式选择，四套主题        │  与系统亮暗解耦
└────────────────────────────────┘
```

**阅读主题必须与系统亮暗解耦。** 用户系统是暗的、但想在护眼棕褐下读书，是很常见的组合。当前代码用 `prefers-color-scheme` 单方面决定阅读区颜色，是错的——`prefers-color-scheme` 只能作为**首次默认值**，不能作为持续约束。

### 3.2 UI 层

| Token | Light | Dark | 用途 |
|---|---|---|---|
| `--ui-bg` | `#F7F5F0` | `#131519` | 页面底 |
| `--ui-surface` | `#FFFDF9` | `#1B1E23` | 卡片、工具栏、抽屉 |
| `--ui-surface-2` | `#F1EEE7` | `#24282E` | 次级面：输入框、未选中按钮 |
| `--ui-text` | `#1F2226` | `#E8E4DC` | 正文 |
| `--ui-text-muted` | `#5C6167` | `#9A9489` | 元信息、页码、说明 |
| `--ui-border` | `#E3DFD6` | `#2E333A` | 分隔线、卡片描边 |
| `--ui-accent` | `#8A5A2B` | `#C9A37A` | 主操作、选中态、链接 |
| `--ui-accent-soft` | `rgba(138,90,43,.10)` | `rgba(201,163,122,.14)` | 选中背景、按下反馈 |
| `--ui-accent-fill` | `#C98A2E` | `#D9903F` | **图形用途**的强调色 |
| `--ui-danger` | `#B91C1C` | `#E0716A` | 删除等破坏性操作 |
| `--ui-overlay` | `rgba(31,34,38,.40)` | `rgba(0,0,0,.55)` | 遮罩 |

**实测对比度**（WCAG，正文目标 4.5:1）

| 组合 | 比值 | 等级 |
|---|---|---|
| `#1F2226` on `#F7F5F0` | 14.66:1 | AAA |
| `#5C6167` on `#F7F5F0` | 5.73:1 | AA |
| `#8A5A2B` on `#FFFDF9` | 5.78:1 | AA |
| `#B91C1C` on `#FFFDF9` | 6.37:1 | AA |
| `#E8E4DC` on `#131519` | 14.41:1 | AAA |
| `#9A9489` on `#131519` | 6.07:1 | AA |
| `#C9A37A` on `#1B1E23` | 7.17:1 | AAA |
| `#E0716A` on `#1B1E23` | 5.36:1 | AA |

> 原先的 `--muted: #6f747b` 在 `#f4f2ee` 上只有 4.1:1，不达标，已替换为 `#5C6167`。

### 3.3 阅读区四套主题

| Token | `paper` 暖纸 | `sepia` 护眼 | `night` 暗纸 | `oled` 纯黑 |
|---|---|---|---|---|
| `--reader-bg` | `#FDFBF7` | `#F4ECD8` | `#14161A` | `#000000` |
| `--reader-surface` | `#EAE6DE` | `#E3D8BE` | `#0E1013` | `#0A0A0A` |
| `--reader-text` | `#1A1A1A` | `#3B3327` | `#B9B5AE` | `#A8A8A8` |
| `--reader-text-muted` | `#6B6B6B` | `#6B6B6B` | `#9A9489` | `#8A857A` |
| `--reader-accent` | `#8A5A2B` | `#8A5A2B` | `#C9A37A` | `#C9A37A` |
| `--reader-rule` | `#E8E3DA` | `#DED2B4` | `#23272D` | `#1A1A1A` |
| 推荐场景 | 日间默认 | 长时间阅读 | 夜间默认 | AMOLED 省电 |

**实测正文对比度**：paper 16.84:1 / sepia 10.56:1 / night 8.87:1 / oled 8.83:1，**四套全部达到 AAA（≥7:1）**。

`--reader-surface` 是给 PDF 用的衬底（页面之间的间隙）。它必须与 `--reader-bg` 有可见差异，否则 PDF 的白色页面会和背景糊成一片，纸感消失。

#### 关键结论：主题相关的语义色不能用单值

`--reader-text-muted` 和 `--reader-accent` **必须随主题切换**，实测数据说明原因：

| 值 | paper 底 | sepia 底 | night 底 |
|---|---|---|---|
| `#9A9489`（暗底用的灰） | **2.91:1 不达标** | **2.56:1 不达标** | 6.01:1 可用 |
| `#6B6B6B`（亮底用的灰） | 5.16:1 可用 | 4.53:1 可用 | 3.40:1 偏弱 |

一个值通吃四套主题是不可能的。**别为了"省 token"把它们合并成一个变量。** 这是接 TXT/EPUB 时最容易踩的坑。

### 3.4 主题选择规则

```css
/* 1. 默认：跟随系统，作为首次默认值 */
.reader { /* light → paper */ }
@media (prefers-color-scheme: dark) { .reader { /* → night */ } }

/* 2. 用户显式选择：data-reader-theme 覆盖，优先级更高 */
.reader[data-reader-theme='sepia'] { ... }
```

用户一旦显式选过，就持续性优先于系统设置，并写入 `db.progress` 持久化。

---

## 4. 字体系统

### 4.1 一个 webfont 都不用

理由见 §2 偏离记录。全部系统字体栈，西文在前、中文在后（浏览器按字符逐个回退，顺序错了会整段回退）。

```css
--font-ui:      system-ui, -apple-system, 'Segoe UI', 'PingFang SC',
                'Hiragino Sans GB', 'Microsoft YaHei', 'Noto Sans CJK SC', sans-serif;
--font-reading-serif: Georgia, 'Times New Roman', 'Songti SC', 'STSong',
                'Source Han Serif SC', 'Noto Serif CJK SC', 'SimSun', serif;
--font-reading-sans:  system-ui, -apple-system, 'PingFang SC', 'Hiragino Sans GB',
                'Microsoft YaHei', 'Noto Sans CJK SC', sans-serif;
--font-mono:    ui-monospace, 'SF Mono', 'Cascadia Mono', Consolas, 'Liberation Mono', monospace;
```

- **UI 一律用 `--font-ui`**（无衬线），包括标题。理由：操作层是仪器，不是书。
- **正文由用户选衬线还是无衬线**（`serif` / `sans`），默认 `serif`——中文长文衬线更耐读。
- **所有数字必须等宽**：页码、百分比、字数用 `font-variant-numeric: tabular-nums`，否则翻页时数字宽度跳动。

### 4.2 字号阶梯

**UI 层**（固定，不随用户偏好变）

| Token | 值 | 用途 |
|---|---|---|
| `--fs-xs` | 11px | 封面角标、徽章 |
| `--fs-sm` | 12px | 页码、元信息、文件大小 |
| `--fs-md` | 13px | 次要按钮、列表项 |
| `--fs-base` | 14px | 卡片标题、抽屉项 |
| `--fs-lg` | 15px | body 基准 |
| `--fs-xl` | 17px | 页面标题 |
| `--fs-2xl` | 22px | 空状态大标题 |

**阅读区**（用户可调，7 档）

`15 / 16 / 18 / 20 / 22 / 24 / 28` px，**默认 18px**（中文手机阅读实测最舒适区间 17–19px）。

配套三个可调滑杆，均持久化：

| 偏好 | 档位 | 默认 |
|---|---|---|
| 字号 `--reader-size` | 15 / 16 / 18 / 20 / 22 / 24 / 28 | 18px |
| 行高 `--reader-leading` | 1.5 / 1.7 / 1.9 | 1.7 |
| 段距 `--reader-para-gap` | 0 / 0.5em / 1em | 0.5em |
| 页边距 `--reader-pad` | 16 / 24 / 40 px | 24px |

> 行高依语言有别：中文最小 1.7（低于此，汉字方块会挤在一起），西文可到 1.5。默认值取中文安全值。

---

## 5. 排版节奏

### 5.1 间距：8px 基准

```css
--sp-1: 4px;   --sp-2: 8px;   --sp-3: 12px;  --sp-4: 16px;
--sp-5: 20px;  --sp-6: 24px;  --sp-8: 32px;  --sp-10: 40px;  --sp-12: 48px;
```

来自 Swiss Modernism 2.0 的 `--base-unit: 8px`。4px 是唯一的半档，用于紧凑控件内部。

### 5.2 行宽（measure）—— 最重要的排版参数

技能的两条硬规范：

- 西文每行 **65–75 字符**
- 中文每行 **30–40 字**

实现方式用 `em` 而非 `px`，让它随字号自动缩放：

```css
--reader-measure: 34em;   /* 中文按 1em/字算，约 34 字 */
```

**大屏必须限制宽度。** 正文横跨 1920px 是阅读器最典型的低级错误。桌面端阅读区两侧留白，正文居中，`max-width: var(--reader-measure)`。

### 5.3 中文排版细节

- **段首缩进 2em**（`text-indent: 2em`）——中文书正文的传统，也用于区分段落
- 段间距与首行缩进**二选一**，不要同时用（同时用会出现"缩进 + 空行"的双重分隔，很挤）
- 标点挤压（`text-spacing-trim`）暂不做，浏览器支持不一，等 V1

---

## 6. 圆角与阴影

### 6.1 圆角

```css
--radius-none: 0;      /* 阅读区：PDF 页、正文块 —— 这是纸 */
--radius-xs:   4px;    /* 进度条、徽章 */
--radius-sm:   6px;    /* 小标记、标签 */
--radius-md:  10px;    /* 按钮、输入框 */
--radius-lg:  14px;    /* 卡片、封面 */
--radius-xl:  18px;    /* 抽屉、浮层 */
--radius-full: 999px;  /* 圆形按钮、头像 */
```

**规则：越靠近内容，圆角越小。** 阅读区 0，容器类 14–18，控件类 10。

### 6.2 阴影：只给真正"浮起来"的层级

Swiss 与 E-Ink 都建议"能不用就不用"。落地为：

| Token | 用途 |
|---|---|
| `--shadow-0: none` | **默认**。卡片、工具栏用 `border` 而非阴影区分 |
| `--shadow-1` | 轻微浮起：卡片悬停 |
| `--shadow-2` | 浮层：下拉、弹窗 |
| `--shadow-3` | 抽屉：从侧边滑出，层级最高 |
| `--shadow-page` | **PDF 单页专用**：模拟纸张投影 |

```css
--shadow-1: 0 1px 2px rgba(31,34,38,.06);
--shadow-2: 0 2px 8px rgba(31,34,38,.08), 0 1px 2px rgba(31,34,38,.04);
--shadow-3: 0 8px 32px rgba(31,34,38,.16);
--shadow-page: 0 1px 3px rgba(31,34,38,.14);
```

暗色模式下阴影几乎不可见，**暗色靠 `--ui-border` 和 `surface` 层级差来区分**，不要靠加深阴影。

---

## 7. 动效

```css
--dur-1: 120ms;   /* 按下反馈、hover */
--dur-2: 200ms;   /* 工具栏显隐、抽屉 */
--dur-3: 320ms;   /* 页面/章节切换 */
--ease-out:    cubic-bezier(.2, 0, 0, 1);
--ease-in-out: cubic-bezier(.4, 0, .2, 1);
```

### 规则

1. **位移 > 淡入**。翻页用 `translateX` 表达"翻过去了"；工具栏用 `translateY` 表达"收起来了"。淡入淡出只用于内容就绪（骨架屏 → 真实内容）。
2. **E-Ink 风格要求"无运动模糊、无淡出"**，所以页面切换的位移要**干脆**，不加模糊滤镜。
3. **`prefers-reduced-motion` 下所有时长归零**，包括抽屉与页面切换——但仍保留最终状态（不能因为禁用动效导致抽屉打不开）。

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    transition-duration: .01ms !important;
    animation-iteration-count: 1 !important;
  }
}
```

4. **禁止**：滚动视差、加载动画超过 800ms、任何自动播放的装饰动效。

---

## 8. 触控与可达性

| 项 | 要求 | 现状 |
|---|---|---|
| 触控目标 | 最小 **44×44 px** | `.btn-icon` 原为 40px，已提升 |
| 点击延迟 | `touch-action: manipulation` | 已有（`-webkit-tap-highlight-color: transparent`） |
| 安全区 | 所有贴边元素用 `env(safe-area-inset-*)` | 已有 |
| 对比度 | 正文 ≥ 4.5:1，目标 7:1 | 已全员达标（见 §3） |
| 焦点可见 | `:focus-visible` 必须有可见指示 | **原先完全缺失，已补** |
| 指针反馈 | 可点元素 `cursor: pointer` | 已有（全局 button） |
| 键盘 | 方向键翻页、Esc 退出 | 待做（V1） |
| 焦点不被遮挡 | `scroll-padding-top` 避开 sticky 栏（WCAG 2.2 AA） | 待做（抽屉打开时需处理） |

### 焦点样式

```css
:focus-visible {
  outline: 2px solid var(--ui-accent);
  outline-offset: 2px;
  border-radius: inherit;
}
```

**`outline` 不要用 `box-shadow` 替代**——`box-shadow` 会被 `overflow: hidden` 裁掉。

---

## 9. 分格式的设计差异

这一节是接 TXT / EPUB 前必须读的。**"纸"的来源不同，主题的作用范围就不同。**

| | PDF | TXT / EPUB（流式） |
|---|---|---|
| 纸来自哪 | **内容自带**——PDF 页面本身是白底 | **主题给的**——文字直接坐在主题底色上 |
| `--reader-bg` 的作用 | 页间衬底（`--reader-surface` 更有用） | 正文背景本身 |
| `--reader-text` 的作用 | 用不到（文字在 canvas 里） | 正文颜色，核心 token |
| 主题切换的效果 | 只改页间间隙和周边 | 改整个正文区 |
| 页边距 | 无效（缩放决定） | 有效 |
| 行高/字号 | 无效（PDF 排版固定） | 有效 |

**推论**：主题切换器对 PDF 只能提供"沉浸包围色"，对 TXT/EPUB 才是真正的"换纸"。UI 上要如实呈现——在 PDF 里不该显示"行高""段距"这类控件的可点击态，应该禁用或隐藏。

---

## 10. 迁移映射

旧变量名保留为**别名**（指向新 token），现有 class 无需改动即可生效：

| 旧变量 | 新 token |
|---|---|
| `--bg` | `var(--ui-bg)` |
| `--surface` | `var(--ui-surface)` |
| `--surface-2` | `var(--ui-surface-2)` |
| `--text` | `var(--ui-text)` |
| `--muted` | `var(--ui-text-muted)` |
| `--border` | `var(--ui-border)` |
| `--accent` | `var(--ui-accent)` |
| `--accent-soft` | `var(--ui-accent-soft)` |
| `--danger` | `var(--ui-danger)` |
| `--reader-bg` | `var(--reader-bg)`（改为主题驱动） |
| `--shadow` | `var(--shadow-1)` |

别名是**过渡措施**。新代码一律直接用新 token；等 TXT/EPUB 接入后统一清理别名层。

### 已修复的不一致

原先主题色在三处不一致，PWA 安装后状态栏会闪色：

| 位置 | 原值 | 现值 |
|---|---|---|
| `index.html` `<meta theme-color>` | `#f6f5f2` | `#F7F5F0` |
| `vite.config.js` manifest `theme_color` | `#f4f2ee` | `#F7F5F0` |
| `vite.config.js` manifest `background_color` | `#f4f2ee` | `#F7F5F0` |
| `styles.css` `--bg` | `#f4f2ee` | `#F7F5F0` |

---

## 11. 待办

按优先级：

- [x] **阅读主题切换** —— 阅读器工具栏的色块按钮，点击循环切换四套主题，偏好存 `localStorage`
- [x] **完整外观设置面板** —— `components/AppearancePanel.jsx`：四套主题并排选 + 字号 / 行高 / 段距 / 页边距滑杆，带实时预览块
- [x] **字号 / 行高 / 段距 / 页边距滑杆** —— 均按 §4.2 档位，持久化为全局默认
- [x] **桌面端行宽限制** —— `.txt-stage` 的 `max-width: calc(34 * var(--reader-size) + var(--reader-pad) * 2)`，
  小屏由 `min(100%, …)` 退化为「没限」；限在 stage 层而不是视口层，是为了让视口宽度恒等于栏宽
- [~] **工具栏重新组织** —— 部分完成：新增 `⋯` 显示面板承载「常亮 / 沉浸 / 主题（/ 外观）」，
  窄屏（≤560px）收起有替代入口的按钮（PDF 的 ± 缩放、TXT 的上下章）。**仍未做的是把书名与页码也收进去**
- [ ] **状态栏颜色跟随阅读主题** —— 需要 JS 动态改 `<meta theme-color>`，当前是静态值
- [ ] **`prefers-reduced-motion` 验证** —— 在真实设备上确认抽屉仍可打开
- [ ] **焦点陷阱** —— 目录抽屉打开时，Tab 不应跑到底层页面（WCAG 2.2 AA 要求焦点不被遮挡）
- [ ] **颜色工具页面** —— 一个只读的 token 展示页，方便校对（可选）

---

## 12. 移动端三项约定（2026-09-23 新增）

这三件事互相牵制，改任何一个都要回来读这一节。

### 12.1 双指缩放：只拦两根手指

手机浏览器的双指缩放是**整页缩放** —— 地址栏和阅读器自己的工具栏会一起放大，
屏幕上能看到的正文反而更少，而且放大后的坐标系和分栏 / 分页模型对不上。
所以阅读器内必须自己接管（`hooks/usePinch.js`）。

**硬约束：`touch-action` 不能设 `none`，监听必须是非 passive 的，且只在
`touches.length >= 2` 时 `preventDefault()`。** 一根手指完全放给浏览器 ——
长按划词靠它，`none` 会把选择一起杀掉（§8 已有同样的教训）。

手势进行中**不重排**：重排是几十毫秒级的开销，跟着手指走会卡。只做 `transform`
的即时反馈，松手才提交。`transform-origin` 必须按当前视口算（`applyPinchFx`）——
横向滚动的内容层宽度可能是视口的几十倍，默认的 `center` 远在屏幕外，
绕着那个点缩放看起来像整块内容飞走了。

| 格式 | 缩放改什么 | 是否吸附 |
|---|---|---|
| TXT | **字号**（流式排版没有「原始尺寸」，transform 放大只会让一屏的字更少） | 是，吸附 §4.2 七档 |
| PDF | 渲染倍率（与工具栏 ± 同一个值） | 否，连续值 |

吸附同时起**节流**作用：档位没变就不重排，手势中不会每帧触发一次分栏计算。

双指手势结束后 400ms 内，既有的「左右滑动翻页」判定必须跳过
（`shouldIgnoreSwipe()`）—— 缩小手势里 `changedTouches[0]` 的位移轻松超过 50px。

### 12.2 沉浸：UI 层与真全屏层必须分开

- UI 层：`.reader.immersive` 藏掉进度条；顶栏只在点中间区时临时浮现（和视频播放器一个套路）。
- 真全屏层：`requestFullscreen()`，把浏览器自己的地址栏也收掉。

**不能绑死**：iPhone 上的 Safari 至今不支持元素全屏 API（只有 iPad 支持）。
绑死的话 iPhone 用户按了按钮毫无反应；拆开后至少能拿到 UI 层，体验是递减而不是失效。

必须接 `fullscreenchange`：用户按 Esc / 系统手势退出全屏时只有这个事件会告诉我们，
不接的话按钮状态会显示「沉浸中」而屏幕其实已经退出全屏。卸载阅读器时也要主动退出 ——
否则书架留在全屏里而且没有退出入口。

沉浸**不去动内容区高度**：TXT 的正文是重新分栏的，给工具栏腾出的那几十像素一旦
变化就要重排、位置锚点也得重算，换来的收益不值得。

### 12.3 常亮：必须处理「后台回来」

Screen Wake Lock（`hooks/useWakeLock.js`）有三个坑：

1. **切到后台时浏览器会强制释放锁，回到前台不会自动恢复。** 不接 `visibilitychange`
   重新申请，「常亮」会悄悄失效且没有任何提示 —— 用户只会觉得这功能时灵时不灵。
2. 申请可能被拒（低电量模式 / 浏览器策略）：一律**静默降级**，不弹错。
3. 锁可能被系统单方面释放，所以必须监听 `release` 把本地引用同步回 `null`，
   否则我们会以为还持有锁而不再申请。

面板上「此设备不支持」这类说明**不要做成禁用按钮** —— 禁用之后用户不知道为什么不能用，
标出来才知道是浏览器的问题而不是 App 坏了。


---

## 13. EPUB 专属的排版约定（2026-09-23 新增）

EPUB 走的是与 TXT **同一套**流式排版（CSS 多栏分页、同一套 `--reader-*` token、
同一套主题），所以 §9 的两栏表对它完全适用。下面只记 EPUB 特有的、改样式前要知道的几条。

### 13.1 章节文本是「重新排」出来的，不是原样渲染

XHTML 在进阅读器之前会被**展平**：块元素各成一段，`class` / `style` 全部剥掉，
再套上我们的 `.txt-p` 体系。含义是 —— **EPUB 自带样式一律不生效，也不该生效**：
读者选的主题与字号必须赢过出版社的排版，否则「换字号」在带样式的书上会时灵时不灵。

于是视觉样式只有一个来源：`styles.css` 里 `.epub-reader` 那一段。那里按
`data-kind` / `data-level` 区分标题层级（1-6 对应 `h1`-`h6`）、引用、图注、诗行、
分隔线；新加元素时**在这里加规则**，不要在解析层注入样式。

### 13.2 列表符号**不进文本**

`epubDom` 展平时列表项只留文字，符号（`•` / `1.`）由 CSS `::before` 画出来。

这是**位置模型的硬约束**，不是审美选择：段落的 `textContent` 必须与算字符偏移用的
文本**逐字一致**，符号一旦进了文本，偏移就会跟着 `::before` 的显示与否漂移，
高亮和笔记全部错位。同理，任何"看起来有但不在文本里"的装饰都只能用伪元素做。

### 13.3 图片：高度不超过一栏

`.epub-reader img { max-height: var(--reader-col-h) }` —— 一栏 = 一页，
图比栏高就会被分页切到两页上，视觉上像图片被裁了一半。

`--reader-col-h` 由阅读器按实测栏高写进 `style`。图片解码是**异步**的，
所以载入完成后必须重算分页（`measureSeq`），否则页数会按"图片还没占位"的版面算，
表现为后面几页的文字叠在一起。

### 13.4 书内链接不能用 `<a href="#…">`

书里的「见第 3 章」这类跳转，在展平时会重写成 `<span data-frag="…">`。
**不能保留成 `<a href="#part2">`**：改了 hash 就等于改了 SPA 路由，点一下书内链接
会把人直接踢出阅读器（书架以为你换了页面）。

`data-frag` 的样式是**虚线下划线** —— 需要与正文的外链（真 `<a>`）区分：
前者是书内跳转，后者是外链（EPUB 里极少，且我们不做浏览器跳转）。

### 13.5 封面缩略图

EPUB 的封面在 OPF 里，可能是 SVG 或任意尺寸的位图。书架只需要一张卡片大小的缩略图，
所以导入时统一**等比缩放并导出 JPEG dataURL** 存进 `books.cover` —— 保证：
书架不依赖原文件就能显示封面（原文件可能已被删除或从备份恢复到新设备），
且整条链路**不联网**（dataURL 而非 objectURL / 远程地址）。

### 13.6 字体栈：不要打包中文字体

与 TXT 同一条：正文用系统字体栈。思源宋体一类的中文字体动辄 10MB+，
打进包里对"文件不出本机、离线可用"这个承诺是净损失 —— 首屏要等字体下载，
而中文字体的显示差异远小于包体积的代价。

---

## 14. 书签（2026-09-23 新增）

书签与高亮是**两种不同的东西**，UI 上也刻意不放在一起：高亮是「划出来的内容」，
书签是「给位置做的标记」。混在一个列表里，用户会以为书签也是高亮的一种。

### 14.1 位置模型：存的是「那一页最上面那段文字」

书签**不存页号**，存的是建立时当前页顶段的字符偏移 —— 与断点续读用的是同一个锚点。

理由和 §9 的分页逻辑同源：页号是排版的产物，不是内容的位置。读者把字号从 18px 调到
20px，原本的第 2 页会变成第 3 页，按页号存的「第 2 页」就指到别处去了。存偏移则
「重新排完之后这一段在第几页」永远算得出来。

| 格式 | 存什么 | 判定「当前页已有书签」 |
|---|---|---|
| PDF | `page`（0 基页号），`charOffset` 恒 0 | 页号相等（固定版式，页号本身稳定） |
| TXT / EPUB | `page` = 章序号，`charOffset` = 章内偏移 | `pageForCharOffset(offset) === 当前页` |

**判定不能反过来比偏移是否相等。** 改字号会重新分栏，同一个偏移可能落到另一页、
也可能不再是页首 —— 只比数值会出现「按钮显示已加，但眼前这一页并没有书签」。
正确的问法是「这个偏移在当前排版下落在第几页」，落在当前页就算它在眼前。
这与 §9 / MEMORY 里那条「锚点段落落在当前显示的那一栏」是同一条判据。

### 14.2 一个按钮，两种状态

顶栏只有**一个**书签图标（`BookmarkButton`），空心 = 未加、实心 = 已加，按一下切换。
不给「加」和「删」两个入口 —— 那样用户得先自己判断当前位置算不算已加过，
而这正是应用该替他算的事。侧栏「书签」页顶部那条按钮用同一套语义，文案随状态变
（`加当前位置` / `取消当前位置`）。

图标用内联 SVG 而不是 emoji：🔖 在 Android / iOS / Windows 上字形差异极大
（有的是彩色方块），而这里要的恰恰是「空心 / 实心」这个可被 CSS 控制填充的对比。

### 14.3 备份的去重键必须带 `charOffset`

```js
const bookmarkKey = (b) => `${b.bookId}|${b.page}|${b.charOffset ?? 0}`
```

漏掉 `charOffset` 的话，同一章里的多个书签会在恢复时被判定成同一条，只剩一个 ——
而「同一章里记好几个位置」正是书签最常见的用法。恢复是**只做加法**的，
所以去重键必须比「书 → 页 / 章」更细。

### 14.4 摘要是冗余信息，不参与定位

`label`（章标题）与 `snippet`（顶段开头 40 字）只为了让列表读起来知道「这是哪儿」。
**定位永远只看 `page` + `charOffset`** —— 摘要会随着标题编辑、正文重排而过时，
拿它去反查位置迟早出错。PDF 不存 `label`：固定版式里「第 N 页」已经是最清楚的位置说明。

## 15. 翻页方式：一次一屏 vs 竖向滚动（2026-09-23 新增）

### 15.1 两种版式都留着的理由

流式正文没有「正确的翻页方式」。一次一屏（CSS 多栏分页）适合一次读完一段、
不想被滚动分心的人；竖向滚动适合把手机当长条纸看、习惯用拇指滑动的人。
**这是阅读习惯的差异，不是新旧之分** —— 所以两者并存，由读者自己选，
默认仍是翻页（老用户的手感不该被版本更新改掉）。

偏好存在 `prefs.readerFlow`（`'page' | 'scroll'`），**按设备**保存，
不是按书 —— 同一个人的翻页习惯不会一本书一变。PDF 例外：固定版式用
`progress.mode` 按书记忆（一本扫描书横排单页、一本小说连续滚动是合理的）。

入口有两个，都指向同一个动作：顶栏那个显示当前版式的按钮（`data-flow`），
以及 `⋯` 面板里的「翻页方式」一行 —— 窄屏上顶栏按钮会挤，面板是它的替代入口。

### 15.2 两种版式只差一个 class

同一份 DOM，靠 `.scroll-flow` 换掉两个属性：

```css
.txt-viewport.scroll-flow { overflow-x: hidden; overflow-y: auto; }
.txt-columns.scroll-flow  { width: 100%; height: auto; column-width: auto; column-count: auto; }
```

分页模式下 `width / height / column-width / column-gap` 是 JS 写上去的内联 style
（分栏分页的输入）；滚动模式下**必须不写**，否则内容还会被切成横向的栏。
所以 React 那边是 `style={flow === 'scroll' ? undefined : {...}}`，
测试也直接断言 `getAttribute('style') === null`。

`.txt-stage` 的 `max-width`（34em 行宽上限）两种模式共用 —— 滚动模式同样要有行宽上限，
「横跨 1920px 读正文」在哪种版式下都是错的。

### 15.3 位置单位从「栏」换成「段」，判据本身不变

分页模式的位置是 `scrollLeft ÷ (栏宽 + 间距)`；滚动模式是「视口顶部压着第几段」。
两者都归到同一个抽象上：**当前位置单位**。

- `pageForCharOffset(偏移)` 在分页下返回栏号，在滚动下返回**段下标**；
- `page` state 在分页下是栏号，在滚动下是段下标。

于是书签的「当前页是否已有书签」、后退栈的「目标是否已在眼前」这些判据一行都不用改 ——
它们问的一直是「目标单位 == 当前单位」（见 §14.3 与 MEMORY 的那条判据）。

**恢复位置、换字号、旋屏、切版式，走的都是同一套锚点**（章 + 章内字符偏移）。
切版式时把当前锚点塞进 `pendingAnchorRef`，重排后的定位副作用再落一次 ——
坐标换了，人不能跟着跳走。端到端断言是「切完版式后锚点段仍落在当前显示的那一栏」。

### 15.4 滚动容器底部必须留白

```css
.txt-columns.scroll-flow { padding-bottom: 50vh; }
```

没有这段留白，「把末尾那一段顶到视口顶部」这个定位动作**物理上做不到**
（`scrollTop` 到不了那么大），每次恢复位置都会悄悄退到前一段 ——
属于「退出再进来位置总是差一点」那一类 bug，而且很难当场看出来。
留白约一屏是够用且不碍眼的量。

### 15.5 边界行为：只在当前章内滚动

滚动模式**不做跨章无限加载**。理由：无限加载会让「这一章还剩多少」失去答案，
而流式格式的进度感恰恰来自章节；同时它要把相邻章一起排版，
图片 objectURL、高亮回填、页数测量的生命周期全都要变成多章窗口，代价远大于收益。

取而代之的是**滚到底再往下走就进下一章**：`PageDown` / 点右侧 1/3 / 横滑
在滚动模式下都解释成「往下滚一屏」，滚到底（或退到章首）时自动换章。
读者得到的手感是连续的，而每一章仍是独立一屏。

### 15.6 底栏滑杆在两种版式下含义不同

分页模式是 `第 N / M 页`（拖到第几栏）；滚动模式没有「第几页」这个概念，
滑杆改成 `第几段`，右侧读数换成**本章百分比** —— 它同时是「我在这一章哪儿」的答案。

顶栏中间那行（`第 N/M 章 · xx%`）两种模式共用，因为章节与百分比与版式无关。

### 15.7 书架的「添加图书」栏平时收起

首屏平铺三个格式按钮有两个问题：占掉书架最上面一整块，以及逼着人先读一遍
「我们支持什么」—— 而这两件事对「已经知道要导入什么」的人都是纯成本。

所以平时只有一行 `＋ 添加图书`，点开才列出 PDF / TXT / EPUB。
两个细节：

- **展开才收窄 `accept`**。收起状态下 `<input>` 的 `accept` 是三种格式的并集；
  点某个格式按钮时才收窄到那一种（手机上文件选择器会照着它过滤）。
  收起时再还原成并集，否则下次点开默认还停在上次那种格式，选不出别的来。
- **拖拽提示改成按需浮现**。常驻的虚线框没了，改成拖拽悬停时才浮出提示条 ——
  桌面端的能力不必占用首屏。整块书架仍然可以接住 drop 事件。

### 15.8 点击＝开关工具栏，翻页不走点击（2026-09-23 二次修正）

旧实现不看工具栏状态，一律按落点分区：左 1/3 上一页、右 1/3 下一页、中间开关菜单。
问题出在工具栏**3.5 秒后会自动收起**这件事上 —— 翻完页工具栏正好是收起状态，
此时想调出工具栏的人不管点哪儿都像在「点空白」，而点在两侧就是翻页，翻完又自动收起。
**于是工具栏永远调不出来**，越着急点得越偏，越偏越翻页。

第一版修正保留了「展开状态下两侧 25% 翻页」，但实测仍然是猜谜：
**同一个动作（点屏幕），在工具栏收起 / 展开、滚动 / 翻页四种组合下行为全不同**，用户记不住。
最终定案为**一条规则，无例外**：

> **点屏幕 = 切换工具栏显隐。任何状态、任何版式、任何位置，都不翻页。**

翻页的出口有三条，互不占用点击：

- **手机**：滑动（翻页模式左右滑换页；滚动模式上下滑浏览，滚到底继续推 → 换节）
- **键盘**：←/→/↑/↓/PageUp/PageDown/空格（滚动模式待支持，见「当前缺口」）
- **鼠标 / 兜底**：底栏固定「‹ ›」两个翻页按钮，随进度条常驻

隐性分区是万恶之源：**永远不要让「同一个点击动作」在不同状态下干不同的事。**
用户点屏幕时心里只有一个意图——「我要么想要工具栏，要么想关掉它」，那就让它只干这件事。

### 15.9 长按选词与系统菜单的冲突

移动端长按选词时，系统会同时弹出自己的「复制 / 全选」气泡，和阅读器的
SelectionPopover 叠在一起。压制方法（两层都要）：

- **CSS**：`.txt-stage { -webkit-touch-callout: none }`（iOS 的长按气泡）
- **JS**：stage 上 `onContextMenu={(e) => e.preventDefault()}`（Android 的长按菜单）

注意**不要**动 `user-select`：选区手柄必须保留，否则用户连选区都调不出来，
划词功能整个废掉。只压「系统菜单」，不压「选择能力」。

### 15.10 进度百分比要实时，不要只看章号

顶栏 / 底栏的百分比旧实现是 `chapterPercent(chapter, total)` —— 只依赖章号，
在同一章里读多久都是同一个数，用户感知就是「进度条不动」。

实时化的口径（TXT / EPUB 各自的算法，形状一致）：

- **TXT**：全书有统一字符坐标（`chapter.start + anchor` ÷ `text.length`），精确到字符
- **EPUB**：章是懒加载的、没有全局字符基点，用 `(当前章 + 章内插值) ÷ 总章数` 估算，
  章内插值 = 锚点 ÷ 本章文本长度

刷新时机挂在**锚点同步**上（page 变化 / 滚动静默 120ms），不另起计时器 ——
位置在哪，百分比就在哪，两者永远同源。底栏滑杆保持可拖拽：
翻页模式拖的是「第几页」，滚动模式拖的是「第几段」。



### 15.11 滚动模式的锚点要细到字符，不能停在段首（2026-09-26）

「一章一大块」的 EPUB 转换件（正文不用 `<p>`、整章一个 `<div>` 靠 `<br>` 换行）会让
段级锚点粒度退化成整章：滚动模式的锚点 = 视口顶部压着的那一段的 `data-start`，
整章只有一段时它恒等于 0 —— 退出再进来永远回到章首，表现为「滚动模式记不住进度」。

修法（只动滚动模式，分页模式位置单位是「栏」，段首粒度已够）：

- **同步**（`lib/txtSel.js` 的 `intraOffsetAtTop`）：用 Range 逐字符二分出
  「顶线压在段内第几个字符」，锚点 = `data-start + 段内偏移`；
- **恢复**（`scrollSegToChar`）：先 `scrollToSeg` 把段顶到视口顶，再按目标字符的
  rect 与视口顶的差值补一次 scrollTop —— 量不出 rect（图片段 / 边界）时落回段首；
- 恢复看门狗的核对判据仍是**段级**（顶段 `data-start` 是否等于目标段），字符级
  差异不触发重滚，否则会在行间抖动。

锚点细到字符后，15.10 的实时百分比在大段书里也随之连续（章内插值不再恒 0），
「恢复落点 = 上次进度条位置」两者同源。TXT 不需要这套：段 = 行，粒度已够。

### 15.12 划词气泡的「点空白关闭」要连选区一起清

SelectionPopover 的全屏 mask 负责点空白关闭，但**原生选区还留着**：阅读器的
点击守卫「选区未折叠就不切换工具栏」会吞掉一切点击，而 pointerup 的 60ms 定时器
看到选区还在又把气泡弹回来 —— 用户点掉气泡、再点屏幕，气泡阴魂不散且工具栏
永远唤不出。修法（TXT / EPUB 同一套）：

- mask / Esc 关闭走 `dismissPopover`：**清掉原生选区 + 记关闭时间戳**；
- `openForSelection` 在关闭后 350ms 内不再重开（挡住 pointerup 定时器的回弹）；
- 点色块 / 复制 / 删除本来就清选区，不受影响。
